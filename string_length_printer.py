# A simple functio  that accepts a string as an argument, calculates its length
# and prints the numeric length alongside the original string.
def me(you):
    length = len(you)
    print(str(length) + ' ' + you)
me('hy')
